# 🚀 Быстрый деплой мультиплеер сервера (5 минут)

## Вариант 1: Через Glitch UI (самый простой) ⭐

### Шаг 1: Откройте Glitch
Перейдите на: **https://glitch.com**

### Шаг 2: Создайте проект
1. Нажмите **"New Project"** (справа вверху)
2. Выберите **"Import from GitHub"**
3. Вставьте: `https://github.com/zhirkoalexander-maker/snakegame`
4. В поле **"Root Directory"** введите: `server`
5. Нажмите **"OK"**

### Шаг 3: Дождитесь установки
- Glitch автоматически установит зависимости
- В логах появится: `Snake Multiplayer Server running on port 3000`
- Это занимает ~30 секунд

### Шаг 4: Получите URL
1. Нажмите кнопку **"Share"** (вверху справа)
2. Нажмите **"Live App"**
3. Скопируйте URL (например: `https://olive-steel-piano.glitch.me`)

### Шаг 5: Проверьте работу сервера
Откройте в браузере: `https://ваш-проект.glitch.me`

Вы должны увидеть:
```json
{
  "status": "online",
  "players": 0,
  "isPlaying": false
}
```

### Шаг 6: Обновите URL в клиенте
Откройте файл `snake_game_web.js` и измените строку 23:

```javascript
const SERVER_URL = 'wss://ваш-проект.glitch.me';
```

**Важно:** используйте `wss://` (не `https://`)!

### Шаг 7: Закоммитьте изменения
```bash
git add snake_game_web.js
git commit -m "Update multiplayer server URL"
git push
```

### Шаг 8: Готово! 🎉
Через 1-2 минуты обновится GitHub Pages и можно играть!

---

## Вариант 2: Через Glitch Remix (еще проще)

### Я создал заготовку проекта:
**https://glitch.com/edit/#!/snake-mp-2player**

1. Нажмите **"Remix"** (вверху справа)
2. Проект скопируется к вам
3. Замените файлы на актуальные из `/server` папки
4. Или просто скопируйте URL и используйте мой сервер

---

## Вариант 3: Командная строка (для продвинутых)

```bash
# Установите Glitch CLI
npm install -g @glitch/cli

# Войдите
glitch login

# Создайте проект
cd server
glitch create snake-multiplayer

# Деплой
git push glitch main
```

---

## Проблемы?

### Сервер засыпает
- Glitch усыпляет бесплатные проекты через 5 минут без активности
- Первое подключение разбудит сервер (~10 секунд)
- Для 24/7 работы нужен платный план

### Не могу подключиться
1. Проверьте, что используете `wss://` (не `https://`)
2. Проверьте статус сервера: откройте `https://ваш-проект.glitch.me`
3. Посмотрите логи в Glitch (кнопка "Logs")

### Ошибка "Room is full"
- Можно только 2 игрока одновременно
- Попросите другого игрока выйти или создайте новый проект

---

## Альтернативы Glitch

### Railway.app (бесплатно 500 часов/месяц)
```bash
npm i -g @railway/cli
railway login
cd server
railway up
```

### Render.com (бесплатно)
1. Зарегистрируйтесь на render.com
2. "New" → "Web Service"
3. Подключите GitHub repo
4. Root Directory: `server`
5. Build: `npm install`
6. Start: `npm start`

---

## Нужна помощь?

Напишите в issues: https://github.com/zhirkoalexander-maker/snakegame/issues

Или просто скажите - я помогу! 🚀
