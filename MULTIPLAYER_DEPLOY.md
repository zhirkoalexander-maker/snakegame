# 🎮 Деплой Online Multiplayer на Glitch

## Шаг 1: Создание проекта на Glitch

1. Перейдите на [glitch.com](https://glitch.com)
2. Нажмите "New Project" → "glitch-hello-node"
3. Назовите проект (например, `snake-multiplayer-server`)

## Шаг 2: Загрузка файлов сервера

1. Удалите все файлы в новом проекте
2. Загрузите из папки `server/`:
   - `server.js`
   - `package.json`
   - `glitch.json`
   - `README.md`

Или используйте кнопку "Import from GitHub" и укажите ваш репозиторий, подпапку `server/`

## Шаг 3: Автоматическая установка

Glitch автоматически:
- Установит зависимости из `package.json`
- Запустит сервер командой `npm start`
- Назначит публичный URL (например, `https://snake-multiplayer-server.glitch.me`)

## Шаг 4: Получение URL сервера

1. В Glitch нажмите "Share" → "Live App"
2. Скопируйте URL (например, `https://snake-multiplayer-server.glitch.me`)
3. Добавьте `wss://` вместо `https://` для WebSocket

Пример: `wss://snake-multiplayer-server.glitch.me`

## Шаг 5: Обновление клиента

Откройте `snake_game_web.js` и измените:

```javascript
const SERVER_URL = 'wss://snake-multiplayer-server.glitch.me'; // Ваш URL
```

на ваш URL от Glitch.

## Шаг 6: Деплой клиента

```bash
git add .
git commit -m "Add online multiplayer support"
git push
```

GitHub Pages автоматически обновится через 1-2 минуты.

## Проверка работы

1. Откройте ваш сайт на GitHub Pages
2. Выберите "🌐 Online Multiplayer"
3. Создайте комнату или присоединитесь к существующей
4. Откройте сайт в другой вкладке/браузере и присоединитесь к той же комнате
5. Начните игру!

## Проверка статуса сервера

Откройте в браузере: `https://ваш-проект.glitch.me/`

Вы должны увидеть:
```json
{
  "status": "online",
  "rooms": 0,
  "totalPlayers": 0
}
```

## Troubleshooting

### Сервер не запускается
- Проверьте логи в Glitch (кнопка "Logs")
- Убедитесь, что все зависимости установлены

### Не могу подключиться из клиента
- Проверьте URL в `SERVER_URL` (должен начинаться с `wss://`)
- Откройте консоль браузера (F12) и проверьте ошибки WebSocket

### Glitch засыпает
- Бесплатный план Glitch усыпляет проекты через 5 минут без активности
- Первое подключение может занять ~10 секунд (wake up time)
- Для 24/7 работы рассмотрите платный план или другой хостинг

## Альтернативные хостинги

### Railway.app
```bash
# Install Railway CLI
npm i -g @railway/cli

# Login
railway login

# Deploy
cd server
railway up
```

### Render.com
1. Создайте Web Service
2. Подключите GitHub repo
3. Root Directory: `server`
4. Build Command: `npm install`
5. Start Command: `npm start`

### Heroku
```bash
heroku create snake-multiplayer
cd server
git push heroku main
```

## Мониторинг

Проверить количество активных комнат и игроков:
```bash
curl https://ваш-проект.glitch.me/
```
